#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_basic() {
        // Basic test to ensure the test setup works
        assert_eq!(1 + 1, 2);
    }

    {% if project_name != "" %}
    #[test]
    fn test_project_name() {
        // Test that project name is properly set
        let name = "{{ project_name }}";
        assert!(!name.is_empty());
    }
    {% endif %}
}